apply plugin: 'com.android.application'

android {
    compileSdkVersion 17

    defaultConfig {
        applicationId "org.dieschnittstelle.mobile.android.activities"
        minSdkVersion 11
        targetSdkVersion 16
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
